import ShowBanner from "../../components/ShowBanner/ShowBanner";
import EpisodeSection from "../../components/Episode/EpisodeSection/EpisodeSection";

const Show = () => {
  return (
    <main className="w-full h-full">
      <ShowBanner />
      <EpisodeSection />
    </main>
  );
};

export default Show;
